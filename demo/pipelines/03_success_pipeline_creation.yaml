apiVersion: tekton.dev/v1beta1
kind: Pipeline
metadata:
  annotations:
    ossc.sigstore.tapestry.dev/transparency: "rekor://my-rekor-log/index"
  name: test-pipeline 
spec:
  tasks:
    - name: goodbye
      taskRef:
        name: goodbye
